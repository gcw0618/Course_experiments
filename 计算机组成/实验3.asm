DATAS SEGMENT
	STRING DB 'HELLO$'
	KEY DB 125,248,16,8,26
	MSG1 DB '   MING WEN:$'
	MSG2 DB '   MI WEN:$'
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    LEA DX,MSG1
    MOV AH,09H
    INT 21H
    LEA DX,STRING
    MOV AH,09H
    INT 21H
    MOV CX,5
    LOOP1:MOV BX,CX
    MOV AL,STRING[BX-1]
    XOR AL,KEY[BX-1]
    MOV STRING[BX-1],AL
    LOOP LOOP1
    LEA DX,MSG2
    MOV AH,09H
    INT 21H
    LEA DX,STRING
    MOV AH,09H
    INT 21H
    MOV CX,5
    LOOP2:MOV BX,CX
    MOV AL,STRING[BX-1]
    XOR AL,KEY[BX-1]
    MOV STRING[BX-1],AL
    LOOP LOOP2
    LEA DX,MSG1
    MOV AH,09H
    INT 21H
    LEA DX,STRING
    MOV AH,09H
    INT 21H
    ;此处输入代码段代码
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START

